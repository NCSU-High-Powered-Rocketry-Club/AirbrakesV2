#main-grid {
    layout: grid;
    grid-size: 1;
    grid-rows: auto 1fr;
    # background: green;
}

#flight-header {
    layout: grid;
    overflow-y: auto;
    grid-size: 3; /* 3 columns */
    grid-rows: auto;
    grid-gutter: 1;
    # background: orange;

    #launch-file-name {
        # background: black;
        text-align: center;
        width: 100%;
    }

    #normal-sim-time {
        text-align: center;
        width: 100%;
    }

    Button {
        min-width: 0;
        border: none;
    }

    #launch-clock {
        # background: blue;
        text-align: center;

        &.blink {
            visibility: hidden;
        }
    }

    #state {
        align: left bottom;
        content-align: left bottom;
        text-align: center;
        width: 100%;
        # background: blue;
    }

    #sim-speed-panel {
        max-height: 1;
        # background: teal;
        Horizontal {
            align: center middle;
        }
    }

    #flight-progress-bar {
        column-span: 3;
        width: 100%;
        # background: green;

        Bar {
            width: 1fr;

            &.standby {
                .bar--bar {
                    color: orange;
                }
            }

            &.motorburn {
                .bar--bar {
                    color: red;
                }
            }

            &.coast {
                .bar--bar {
                    color: yellow;
                }
            }

            &.freefall {
                .bar--bar {
                    color: green;
                }
            }

            &.landed {
                .bar--bar {
                    color: indigo;
                }
            }
        }
    }
}

#flight-information-panel {
    layout: grid;
    grid-size: 3;  /* 3 columns */
    # grid-rows: auto;
    grid-rows: 1fr;
    # grid-columns: auto;
    height: 100%;
    overflow: auto;
    # background: maroon;

    #flight-data-panel {
        border: round;
        border-title-align: center;
        overflow-y: auto;
        row-span: 2;
        height: 100%;

        #flight-telemetry-grid{
            grid-size: 5;  /* 5 columns */
            grid-columns: auto 10 auto auto auto;
            grid-rows: auto;
            grid-gutter: 0 1;
            height: auto;

            /* make the numbers in the telemetry grid yellow */
            Label {
                color: yellow;
            }
        }

        #debug-telemetry {
            overflow: auto;
            border-top: round;
            border-title-align: center;

            #debug-telemetry-grid{
                grid-size: 3;  /* 3 columns */
                grid-columns: auto auto 1fr;
                grid-rows: auto;
                grid-gutter: 0 1;
                height: auto;

                #invalid-fields-label{
                    column-span: 2;
                }

                /* make the numbers in the debug telemetry grid yellow */
                Label {
                    color: yellow;
                }
            }

            #packet-grid {
                align: center middle;
                text-align: center;
                grid-size: 4;  /* 3 columns */
                grid-columns: 1fr;
                border-top: solid;
                border-bottom: solid;
                grid-rows: auto;
                grid-gutter: 0 1;
                height: auto;

                /* aligning the text in the center of the grid */
                Label {
                    text-align: center;
                    width: 100%;
                }

                Static {
                    text-align: center;
                    width: 100%;
                }
            }


            #cpu_usage_widget {
                layout: grid;
                border-top: round;
                /* 4 Processes = 4 columns */
                grid-size: 4 3;
                overflow: auto;
                grid-gutter: 1;
                grid-columns: 1fr;
                grid-rows: auto;

                CPUBars {
                    max-height: 10;
                    content-align: center middle;
                }

                CPUBar {
                    color: grey 50%;
                    margin: 0;
                    padding: 0;
                    text-align: center;

                    &.active {
                        &.low {
                            color: white 80%;
                        }

                        &.medium {
                            color: yellow 80%;
                        }

                        &.high {
                            color: red 80%;
                        }
                    }
                }

                Label {
                    content-align: center middle;
                    text-align: center;
                }
            }
        }
    }

    #flight-graph-panel {
        # height: 100;
        # background: orange;
        border-top: round;
        border-left: round;
        border-right: round;
        border-title-align: center;

        PlotWidget {
            # background: yellow;
            height: auto;
        }

        TabbedContent #--content-tab-accel-tab {
            color: red;
        }
        TabbedContent #--content-tab-vel-tab {
            color: blue;
        }
        TabbedContent #--content-tab-alt-tab {
            color: green;
        }
        TabbedContent #--content-tab-pred-apogee-tab {
            color: purple;
        }

    }

    #downrange-map-panel {
        border-bottom: round;
        border-left: round;
        border-right: round;
        border-top: solid;
        border-title-align: center;

        #downrange-map-widget {
            height: 96%;
            width: 96%;
        }

        #x-distance-label {
            margin-left: 4;
            color: orange;
        }

        #y-distance-label {
            color: orange;
            margin-left: 4;
            margin-right: 4;
        }

        #horizontal-range-label {
            color: green;
        }
    }
}

Placeholder {
    width: auto;
    height: auto;
}
